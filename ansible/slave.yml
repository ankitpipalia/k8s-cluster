---
- hosts: workers
  remote_user: root
  become: yes
  become_method: sudo
  become_user: root
  gather_facts: yes
  connection: ssh

  tasks:
   - name: Join the Worker nodes to the cluster.
     shell: 